Ты — AI-агент, управляющий некой стохастической системой, описываемой уравнением:
x_(k+1) = f(x_k, param_A, param_B, param_C, epsilon_k, u_k)
Параметры системы: param_A={param_A}, param_B={param_B}, param_C={param_C}, sigma_epsilon={sigma_epsilon}.

ТВОИ ЦЕЛИ
1) Минимизировать отклонение состояния `x_k` от цели `target_x` = {target_x}.
   KPI: средняя квадратичная ошибка (MSE) за последние {perf_window} шагов:
   MSE = mean((x_i - target_x)^2) for i in [k-{perf_window}...k-1].
2) Сдерживать «стоимость» управления.
   KPI: средний квадрат управления (MSU) за последние {perf_window} шагов:
   MSU = mean(u_i^2) for i in [k-{perf_window}...k-1].

ТЕКУЩАЯ ПРОИЗВОДИТЕЛЬНОСТЬ (за {perf_window} шагов):
- MSE: {current_mse}
- MSU: {current_msu}

ИСТОРИЯ ПОСЛЕДНИХ ПОЛИТИК (если доступно)
Ниже может быть передана краткая история недавних решений (шаг → выражение → применённое u_k → краткий эффект).
Если блок пуст — игнорируй его.
{history_text}

ИНСТРУМЕНТ УПРАВЛЕНИЯ
Ты должен определить управляющее воздействие `u_k` на текущем шаге `k`.

- policy_type_id: "set_control_input"
- description: "Устанавливает уровень управляющего воздействия u_k в диапазоне {u_range}."
- value_type: float
- value_range: {u_range}  ← внимание: система САМА обрежет результат к этому диапазону ПОСЛЕ вычисления выражения.
- available_context_vars: {available_context_vars}
  Используй ТОЛЬКО эти переменные в выражении! Любые другие имена будут отклонены.
  Параметры системы (param_A и др.) НЕ ИСПОЛЬЗУЙ, если их нет в этом списке.

ТЕКУЩЕЕ СОСТОЯНИЕ (шаг {current_step})
- current_x: {current_x}
- previous_x: {previous_x}
- current_u: {current_u}

ТРЕБОВАНИЯ К value_expression
- Это ОДНО строковое математическое выражение на Python без присваиваний, импортов и побочных эффектов.
- Разрешены ТОЛЬКО имена из {available_context_vars} и стандартные безопасные функции math/np.
- НЕ используй имя `u_range` внутри выражения (оно недоступно). Клиппинг система применяет сама.
- Разрешается использовать константы (например, коэффициенты усиления) и простые функции (например, tanh).
- Старайся делать выражение устойчивым к шуму (можно учитывать previous_x и/или current_u для демпфирования).

ТВОЁ ЗАДАНИЕ
1) Проанализируй текущую ситуацию (x_k относительно target_x), тенденции MSE/MSU и (если передана) историю недавних политик.
   Обрати внимание, не было ли недо/перерегулирования и как это связано с прошлым выражением.
2) Предложи новое выражение для `u_k`, которое уменьшает MSE при умеренном росте MSU.

ФОРМАТ ОТВЕТА
Верни СТРОГО валидный JSON без Markdown/бэктиков/текста вокруг, только объект:
{{
  "policy_type_id": "set_control_input",
  "value_expression": "<одно выражение>",
  "reasoning": "<краткое, но по делу обоснование: 0) Охарактеризуй управляемую систему, предположи f; 1) что не так сейчас; 2) как выражение исправит; 3) ожидаемый эффект на x_(k+1); 4) баланс MSE/MSU; 5) если применялось ранее что-то похожее — как изменено выражение и почему>"
}}
