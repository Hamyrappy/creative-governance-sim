# .env template — copy to `.env` in the repo root and fill in (never commit the real .env).
#
# The LLM engine is OpenAI-*compatible* (any provider / local model / proxy), selected purely by
# base_url + model — NOT hardcoded. Only live/cache LLM runs read a key; key-free baselines, the
# replay tape, and `uv run pytest` need none of this.

# --- which key env var + endpoint the client uses ---
OPENAI_API_KEY="YOUR KEY GOES HERE"     # the actual key (or set OPENAI_API_KEY_ENV to point elsewhere)
# OPENAI_API_KEY_ENV="AIRI_KEY"         # optional: name of the env var holding the key (default OPENAI_API_KEY)
# OPENAI_BASE_URL="https://openrouter.ai/api/v1"   # optional: OpenRouter / vLLM / Ollama / a proxy
OPENAI_MODEL="gpt-4o-mini"              # the model id passed to the endpoint

# --- reproducibility / cost knobs (see govsim/core/llm/cache.py) ---
GOVSIM_LLM_MODE="cache"                 # live | cache | replay  (replay = never calls the network)
# GOVSIM_LLM_CACHE="logs/llm_cache"     # where the cache/replay tape lives
# GOVSIM_LLM_MAX_TOKENS="512"           # optional per-provider cap
# GOVSIM_LLM_REASONING_EFFORT="low"     # optional (e.g. gpt-oss, so it does not over-think)
